github:
  enabled: true
  repo: "swebuild/redrawn"
  label: "task-request"
  workspace_path: "/root/tbrain"
  poll_interval_seconds: 300
  priority_labels: ["priority:urgent", "priority:high", "P0", "P1"]
  exclude_labels: ["wontfix", "duplicate"]
